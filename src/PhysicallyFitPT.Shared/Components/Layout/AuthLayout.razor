@* Minimal layout for authentication pages (Login, Logout) without navigation *@
@inherits LayoutComponentBase

<div class="auth-layout">
  @Body
</div>

@code {
  // No navigation, no header, no user context — just the auth content
}
