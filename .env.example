# Where collected context lands
OUT_DIR=reference

# Skip files larger than N MB during ingest
MAX_FILE_MB=5

# Optional: run your indexer after ingest (or use `make index`)
# Example (SQLite embeddings indexer):
# INDEX_CMD=dotnet run --project src/RepoMemory.Indexer -- reference reference/memory.db
INDEX_CMD=
